# exam1

파이썬을 데이터 수집

urllib,requests 패키지 이용하여 html,xml 등의 정보 받아옴

bs4의 BeutifulSoup 패키지를 사용하여 콘텐츠,태그명,속성값,텍스트 등을 받아옴.

'^b' : b로 시작하는~    <-> 'b$' : b로 끝나는 ~ 태그명 or 속성값 선택한다는 뜻 

bs.find_all() 을 사용하여 여러 값에 해당하는 객체를 리턴할 수 있다. -> limit은 해당하는 것에 몇개까지 가져올 지, recursive는 자손까지 포함할 것인지 등의 설정을 하여 구해올 수 있다.

bs.find() -> 한 개만 찾는 것 find_all에 limit=1을 준 것이라 똑같음 

bs.ul.li.string과 bs.ul.li.text에는 차이가 있다. 

exam5 -> googlemap
exam7 - folium
표준화 : 이상치 제거 -> 다만 이상치가 중요한 의미를 가지고 있는 경우가 있으므로 신중해야한다.
정규화 : 수치 크기 다를 시 차이 제거 



## exam12~15 머신러닝, 예제 

